<%= form_for([@person, @person.notes.build]) do |f| %>
  <div class="form-group row">
    <div class="col-md-4">
      <%= f.text_area :body, class: 'form-control'%>
    </div>
  </div>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
